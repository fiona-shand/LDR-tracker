export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Availability windows</h1>
      <p className="mt-2 text-sm text-gray-500">
        Once calendars are connected, upcoming windows when you&apos;re both
        free will show up here.
      </p>
    </div>
  );
}
